import { ensureArtifactToolWorkspace, importArtifactTool } from '/Users/www1/.codex/plugins/cache/openai-primary-runtime/presentations/26.521.10419/skills/presentations/scripts/artifact_tool_utils.mjs';
const workspace = '/Users/www1/Desktop/stat418-tools-in-datascience-2026/final-project/outputs/manual-targeted-ppt-link/presentations/link-edit';
const pptxPath = '/Users/www1/Desktop/stat418-tools-in-datascience-2026/final-project/final-slides/Mike_Wu_Final.pptx';
await ensureArtifactToolWorkspace(workspace);
const artifact = await importArtifactTool(workspace);
const { FileBlob, PresentationFile } = artifact;
const presentation = await PresentationFile.importPptx(await FileBlob.load(pptxPath));
console.log('presentation keys', Object.keys(presentation));
console.log('slides', presentation.slides, Object.keys(presentation.slides ?? {}), presentation.slides?.length);
const slide = presentation.slides?.[0] ?? presentation.slides?.at?.(0);
console.log('slide type', typeof slide, slide && Object.keys(slide));
console.log('slide shapes', slide?.shapes, slide?.shapes && Object.keys(slide.shapes), slide?.shapes?.length);
console.log('first shape keys', slide?.shapes?.[0] && Object.keys(slide.shapes[0]));
